upstream backend {
    server backend_1:3000 max_fails=2 fail_timeout=30s;
    server backend_2:3000 max_fails=2 fail_timeout=30s;
    server backend_3:3000 backup;
}

server {
    listen 80;

    location / {
        proxy_pass http://backend;
    }
}
